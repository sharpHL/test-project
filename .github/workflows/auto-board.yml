name: Auto Add to Board

on:
  issues:
    types: [opened]

jobs:
  add-to-board:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/add-to-project@v1.0.2
        with:
          # Replace https://github.com/users/sharpHL/projects/3 with your GitHub Project Board URL
          # e.g. https://github.com/users/USERNAME/projects/1
          project-url: https://github.com/users/sharpHL/projects/3
          github-token: ${{ secrets.BOARD_TOKEN }}
